services:
  academic-source:
    build: .
    ports:
      - "8000:8000"
    environment:
      ACADEMIC_SOURCE_DATA_DIR: /data/academic-source
    volumes:
      - academic-source-data:/data/academic-source
    restart: unless-stopped

volumes:
  academic-source-data:
