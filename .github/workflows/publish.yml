name: Publishing paused

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publishing is disabled for this fork. Configure an owned package namespace before enabling releases."
